import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PasswordStrength from './App';

ReactDOM.render(
  <React.StrictMode>
    <PasswordStrength />
  </React.StrictMode>,
  document.getElementById('root')
);
